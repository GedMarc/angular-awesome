# Zoomable Frame examples

```html
<wa-zoomable-frame
  src="/documents/preview.html"
  label="Document preview"
  name="preview"
  allow="fullscreen"
  sandbox="allow-scripts allow-same-origin"
  (load)="onPreviewLoaded()">
</wa-zoomable-frame>
```
